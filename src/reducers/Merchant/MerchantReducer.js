import Action from '../../actions/index';
var initialState = {
    
}
function Merchant(state = initialState,action){
    switch (action.type) {
       
        default:
            return state;
    }
}
export default Merchant;